import React from 'react';
import PagePlaceholder from './PagePlaceholder';

const History: React.FC = () => {
  return <PagePlaceholder title="History" icon="history" />;
};

export default History;
